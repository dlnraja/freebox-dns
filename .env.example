# Copy to .env and adjust. Never commit .env (may contain LAN IPs you prefer private).

# --- Host / LAN ---
# IP of the machine running Docker (Freebox VM, Pi, or WSL host LAN IP).
HOST_IP=192.168.1.50

# Lab ports (default) — change to 53/443 only when nothing else binds them.
DNS_LIBRE_PORT=5353
DNS_SECURE_PORT=5354
DOH_LIBRE_PORT=8443
DOH_SECURE_PORT=8444
BLOCKY_HTTP_PORT=3080

# --- Pinned Freebox / LAN DNS fallbacks (captured once; do not remote-poll) ---
# From config/freebox-dns-snapshot.json on this Freebox v9 LAN (2026-09-19).
FREEBOX_DNS_1=91.239.100.100
FREEBOX_DNS_2=185.95.218.42
FREEBOX_DNS_3=9.9.9.10
FREEBOX_DNS_4=45.90.28.0
FREEBOX_DNS_5=192.168.1.254

# Uncensored DoT upstreams for unbound / blocky (not political filter lists)
UPSTREAM_DOT_1=tls://dns.quad9.net
UPSTREAM_DOT_2=tls://dns.digitale-gesellschaft.ch
UPSTREAM_DOT_3=tls://uncensoreddns.org
