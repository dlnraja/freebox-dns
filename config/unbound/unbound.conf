# Reference / docs only — the running container uses the mvance/unbound
# generated unbound.conf + our mounted forward-records.conf.
# See docker-compose.yml volume for unbound.
#
# Amonts DoT non censeurs : config/unbound/forward-records.conf
