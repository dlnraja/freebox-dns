# Uncensored DoT forwarders for dns-libre (mounted into mvance/unbound)
# Replaces Cloudflare defaults — no operator/political filter resolvers.
forward-zone:
  name: "."
  forward-tls-upstream: yes
  # Quad9 unblocked (+ ECS)
  forward-addr: 9.9.9.10@853#dns.quad9.net
  # Digitale Gesellschaft (CH)
  forward-addr: 185.95.218.42@853#dns.digitale-gesellschaft.ch
  # UncensoredDNS
  forward-addr: 91.239.100.100@853#anycast.uncensoreddns.org
